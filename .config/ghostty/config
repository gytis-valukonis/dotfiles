# WezTerm: config.color_scheme = "nord"
# Ghostty uses "theme" (built-in or custom).
theme = Nord

# WezTerm: config.font = wezterm.font("GeistMono Nerd Font Mono")
font-family = "GeistMono Nerd Font Mono"

shell-integration-features = no-title

window-padding-x = 10
